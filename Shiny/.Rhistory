setwd("~/GitHub/strava/Shiny")
shiny::runApp()
runApp()
library(tidyverse)
if(!("data_load" %in% ls())) data_load <- read_csv("../data/all_activities_df.csv")
if(!("data_load" %in% ls())) data_load <- read_csv("../data/all_activities_df.csv")
data_load %>% group_by(data_load) %>% summarize(across(activity_id, ~ mean(.)))
data_load %>% group_by(activity_id) %>% summarize(across(watts, ~ mean(.)))
data_load %>% group_by(activity_id) %>% summarize(across(watts, ~ mean(.)))
data_load %>% group_by(activity_id) %>% summarize(across(watts, ~ mean(., na.rm=T)))
summary_watts <- data_load %>% group_by(activity_id) %>% summarize(across(watts, ~ mean(., na.rm=T)))
View(summary_watts)
summary_watts %>% filter(which.max(watts))
summary_watts %>% filter(watts==max(watts))
max(summary_watts$watts)
max(summary_watts$watts, na.rm = F)
max(summary_watts$watts, na.rm = T)
summary_watts %>% filter(watts==max(watts), na.rm=T)
summary_watts %>% filter(watts==max(watts, na.rm=T))
quantile(summary_watts$watts, .9)
summary_watts %>% filter(watts > quantile(watts, .9, na.rm = T))
summary_watts %>% filter(watts > quantile(watts, .95, na.rm = T))
summary_watts %>% filter(watts > quantile(watts, .95, na.rm = T)) %>% arrange(watts)
?arrange
summary_watts %>% filter(watts > quantile(watts, .95, na.rm = T)) %>% arrange(desc(watts))
source("~/GitHub/strava/Shiny/functions.R", echo=TRUE)
watts_summary <- summarize_watts(data_load, quantile = 0)
watts_summary
watts_summary <- summarize_watts(data_load, quantile = 0.9)
watts_summary
shiny::runApp()
shiny::runApp()
runApp()
runApp()
